import React from "react";
import { Button } from "@material-ui/core";

export const AppView = () => {
	return (
		<Button>
			sometimes I have to make this way too long in order to test things
		</Button>
	);
};
